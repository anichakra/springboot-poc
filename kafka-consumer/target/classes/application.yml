server:
  port: 8065
  
spring:  
  application:
    name: kafka-consumer
  
eureka:  
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER}
  instance:
    leaseRenewalIntervalInSeconds: 1
    leaseExpirationDurationInSeconds: 2
          
ribbon:
  eureka:
    enabled: true
    
management:
  security:
    enabled: false