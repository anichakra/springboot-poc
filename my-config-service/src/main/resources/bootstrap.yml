server:
  port: 8888
  connection-timeout: 5000
spring:
  cloud:
    config:
      server:
        git:
          uri: file://${git.uri}
          default-label: ${git.branch}
  application:
    name: configuration
